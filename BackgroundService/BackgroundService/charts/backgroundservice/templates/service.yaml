apiVersion: v1
kind: Service
metadata:
  name: {{ template "backgroundservice.fullname" . }}
  labels:
    app: {{ template "backgroundservice.name" . }}
    chart: {{ template "backgroundservice.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "backgroundservice.name" . }}
    release: {{ .Release.Name }}
